# Frontend environment example - copy to client/.env
VITE_API_URL=http://localhost:3000
# Optional for Google sign-in (not required for guest-mode demo)
VITE_GOOGLE_CLIENT_ID=<your-google-client-id>
# Optional Supabase vars (not used in guest-mode)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
