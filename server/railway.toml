[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

[[services]]
name = "openaichain-backend"
autoDeploy = true

[services.env]
NODE_ENV = "production"